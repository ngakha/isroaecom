{
  "name": "prshark-ecommerce-template",
  "version": "1.0.0",
  "description": "PRShark Ecommerce Engine Template - Headless ecommerce platform",
  "private": true,
  "workspaces": ["server", "admin", "storefront"],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:admin\" \"npm run dev:storefront\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:storefront": "npm run dev --workspace=storefront",
    "build": "npm run build --workspace=admin && npm run build --workspace=storefront",
    "build:storefront": "npm run build --workspace=storefront",
    "migrate": "npm run migrate --workspace=server",
    "migrate:rollback": "npm run migrate:rollback --workspace=server",
    "seed": "npm run seed --workspace=server",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
